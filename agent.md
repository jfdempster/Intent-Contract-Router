# agent.md

Codex uses `AGENTS.md`, not this filename, for project instruction discovery. This file exists because the scaffold request asked for `agent.md`; keep it as a pointer only.

See `AGENTS.md` for mandatory Intent Contract Router entrypoint, gates, and commands.
